
export type BootstrapGitOptions = {
  useGit: boolean,
  username?: string,
  repository?: string,
  initGit?: boolean,
  addOrigin?: boolean,
  add?: boolean;
  commit?: boolean;
  push?: boolean;
}
